# ConnectTheDots 项目多语言实现文档

## 📋 概述

本项目使用 **next-intl** 库实现国际化(i18n)功能,支持多语言切换。

---

## 🏗️ 技术架构

### 核心依赖
- **next-intl**: Next.js 国际化解决方案
- **Next.js App Router**: 使用 `[locale]` 动态路由

### 支持的语言
- `en` - 英语 (默认语言)
- `de` - 德语
- `es` - 西班牙语

---

## 📁 项目结构

```
connextthedots/
├── i18n/
│   ├── routing.ts          # 路由配置和导航组件
│   └── request.ts          # 请求配置和消息加载
├── messages/
│   ├── en.json            # 英语翻译文件
│   ├── de.json            # 德语翻译文件
│   └── es.json            # 西班牙语翻译文件
├── middleware.ts          # 中间件配置
└── app/
    └── [locale]/          # 动态语言路由
```

---

## ⚙️ 配置文件详解

### 1. i18n/routing.ts
```typescript
import { defineRouting } from 'next-intl/routing';
import { createNavigation } from 'next-intl/navigation';

export const routing = defineRouting({
  locales: ['en', 'de', 'es'],
  defaultLocale: 'en',
  localePrefix: 'as-needed'
});

export const { Link, redirect, usePathname, useRouter } = createNavigation(routing);
```

**作用**:
- 定义支持的语言列表
- 设置默认语言
- 导出国际化的导航组件

### 2. i18n/request.ts
```typescript
import { getRequestConfig } from 'next-intl/server';

const locales = ['en', 'de', 'es'];
const defaultLocale = 'en';

export default getRequestConfig(async ({ requestLocale }: any) => {
    const locale = await requestLocale;
    const safeLocale = locales.includes(locale as any) ? locale : defaultLocale;

    return {
        locale: safeLocale,
        messages: (await import(`../messages/${safeLocale}.json`)).default
    };
});
```

**作用**:
- 根据请求的语言加载对应的翻译文件
- 提供语言回退机制

### 3. middleware.ts
```typescript
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  matcher: ['/', '/(de|en|es)/:path*', '/((?!api|_next|_vercel|.*\\..*).*)']
};
```

**作用**:
- 拦截所有请求并处理语言路由
- 排除 API、静态文件等路径

---

## 📝 翻译文件结构

所有翻译文件位于 `messages/` 目录,采用 **JSON 格式**,按功能模块组织:

```json
{
  "hero": {
    "badge": "✨ Free Dot to Dot Generator ✨",
    "titleMain": "The Best Free Connect the Dots",
    "titleHighlight": "Generator & Maker"
  },
  "header": {
    "logo": "ConnectTheDots",
    "home": "Home",
    "allPrintables": "Free Printables"
  },
  "footer": {
    "description": "Your source for high-quality printables"
  }
}
```

### 主要模块
- `metadata` - SEO 元数据
- `hero` - 首页英雄区
- `header` - 页头导航
- `footer` - 页脚
- `howToGuide` - 使用指南
- `instantDownloads` - 即时下载区
- `categories` - 分类区
- `aiFeatures` - AI 功能介绍
- `editorPicks` - 编辑推荐
- `features` - 功能特性
- `demo` - 演示区
- `printingGuide` - 打印指南
- `faq` - 常见问题
- `contact` - 联系方式
- `customGenerator` - 自定义生成器
- `editor` - 编辑器
- `pricing` - 价格方案

---

## 💻 代码使用示例

### 在组件中使用翻译

```typescript
"use client";

import { useTranslations } from "next-intl";

export default function MyComponent() {
    // 加载 header 模块的翻译
    const t = useTranslations("header");

    return (
        <div>
            <h1>{t("logo")}</h1>
            <nav>
                <a href="/">{t("home")}</a>
                <a href="/printables">{t("allPrintables")}</a>
            </nav>
        </div>
    );
}
```

### 使用国际化路由

```typescript
import { Link } from "@/i18n/routing";

export default function Navigation() {
    return (
        <nav>
            {/* 自动添加语言前缀 */}
            <Link href="/">Home</Link>
            <Link href="/pricing">Pricing</Link>
        </nav>
    );
}
```

### 获取当前语言

```typescript
import { useLocale } from "next-intl";

export default function LanguageSwitcher() {
    const locale = useLocale(); // 返回 'en', 'de', 或 'es'

    return <div>Current language: {locale}</div>;
}
```

---

## 🔄 URL 路由规则

### 默认语言 (en)
- ✅ `/` → 英语首页
- ✅ `/pricing` → 英语价格页

### 其他语言
- ✅ `/de` → 德语首页
- ✅ `/de/pricing` → 德语价格页
- ✅ `/es` → 西班牙语首页
- ✅ `/es/pricing` → 西班牙语价格页

---

## 🎯 关键特性

1. **自动语言检测**: 根据浏览器语言自动跳转
2. **SEO 友好**: 每个语言独立 URL
3. **类型安全**: TypeScript 支持
4. **服务端渲染**: 支持 SSR 和 SSG
5. **回退机制**: 缺失翻译时使用默认语言

---

## 📊 当前翻译覆盖率

| 语言 | 文件 | 键数量 | 状态 |
|------|------|--------|------|
| 英语 (en) | en.json | ~280+ | ✅ 完整 |
| 德语 (de) | de.json | ~280+ | ✅ 完整 |
| 西班牙语 (es) | es.json | ~280+ | ✅ 完整 |

---

## 🚀 添加新语言步骤

1. 在 `i18n/routing.ts` 添加语言代码
2. 在 `i18n/request.ts` 添加语言代码
3. 在 `middleware.ts` 更新 matcher
4. 创建 `messages/[新语言].json` 文件
5. 复制 `en.json` 内容并翻译

---

## ⚠️ 注意事项

1. **JSON 格式**: 必须是有效的 JSON,不能有尾随逗号
2. **键名一致**: 所有语言文件的键名必须完全一致
3. **HTML 标签**: 可以在翻译中使用 HTML,如 `<strong class="text-brand-blue">2 Free Credits</strong>`
4. **占位符**: 暂不支持动态占位符,需要在代码中处理
5. **嵌套结构**: 支持多层嵌套,但建议不超过 3 层

---

## 🔍 调试技巧

### 查看缺失的翻译键
在开发模式下,next-intl 会在控制台显示缺失的翻译键警告。

### 测试不同语言
- 访问 `/en/页面路径` 测试英语
- 访问 `/de/页面路径` 测试德语
- 访问 `/es/页面路径` 测试西班牙语

---

## 📚 相关资源

- [next-intl 官方文档](https://next-intl-docs.vercel.app/)
- [Next.js 国际化指南](https://nextjs.org/docs/app/building-your-application/routing/internationalization)
